@model IEnumerable<ZerodaTrade.Models.Script>

<div style="width:100%; max-height:60vh; overflow-y:auto;">
<ul class="list-group">
@foreach(var s in Model){
    <li class="list-group-item">
        <a href="#" class="stock-link" data-instrument="@s.Name">@s.Name</a>
    </li>
}
</ul>
</div>
